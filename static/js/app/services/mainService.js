// Generated by CoffeeScript 1.9.1
(function() {
  'use strict';
  var BASE_API, mainService;

  BASE_API = 'http://127.0.0.1:8000/api';

  mainService = function($http) {
    this.getAll = function() {
      return $http.get(BASE_API + '/drafts/');
    };
    this.getUser = function() {
      var token;
      token = $window.localStorage.get('token');
      if (token) {
        return JSON.parse(atob(token.split('.')[1]));
      } else {
        return void 0;
      }
    };
  };

  angular.module('main').service('mainService', ['$http', '$window', mainService]);

}).call(this);
