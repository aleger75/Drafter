// Generated by CoffeeScript 1.9.1
(function() {
  'use strict';
  var PARTIALS_DIR, registerController, signInController;

  PARTIALS_DIR = '/static/js/app/auth/';

  signInController = function(authService) {
    this.user = {};
    this.signin = function() {
      authService.signin(this.username, this.password);
    };
  };

  registerController = function(authService) {
    this.register = function() {
      authService.register(this.username, this.password, this.firstName, this.lastName, this.mail);
    };
  };

  angular.module('auth', []).config([
    '$stateProvider', function($stateProvider) {
      $stateProvider.state('/accounts/signin/', {
        url: '/accounts/signin/',
        templateUrl: PARTIALS_DIR + 'signin.html',
        controllerAs: 'signin',
        controller: 'signInController'
      }).state('/accounts/register/', {
        url: '/accounts/register/',
        templateUrl: PARTIALS_DIR + 'register.html',
        controllerAs: 'register',
        controller: 'registerController'
      });
    }
  ]).controller('signInController', ['authService', signInController]).controller('registerController', ['authService', registerController]);

}).call(this);
